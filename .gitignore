.env
core/strimserver.env
tools/local-encoder/local-encoder.env
deploy/aws/twitch-stream-key
deploy/aws/*.key

# Bazel output symlinks (point outside the repo; nothing here to commit).
/bazel-*

# ffmpeg-dist / offline-segment build artifacts
tools/ffmpeg-dist/out.log
tools/ffmpeg-dist/ffmpeg-*.tar.gz
tools/ffmpeg-dist/1

# python caches / local-only obs paths
deploy/aws/__pycache__/
tools/local-obs/

# Local implementation plans — throwaway working docs; not part of the repo.
plans/
playbooks/

# check-deps version-check cache (populated by later phases).
tools/check-deps/.cache/

# Compiled check-deps binary (built locally via Bazel; not committed).
tools/check-deps/strimserver-check-deps
